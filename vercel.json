{
    "builds": [
    {
      "src": "frontend/package.json",
      "use": "@vercel/node",
      "config": {
        "distDir": "frontend/dist"
      }
    }],
    "rewrites": [
      {
        "source": "/api/:path*", 
        "destination": "https://fauna-lmsh-timer.fly.dev/api/:path*"
      }
    ]
}